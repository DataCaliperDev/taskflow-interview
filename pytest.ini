[pytest]
# Pin the asyncio fixture loop scope so pytest-asyncio doesn't emit the
# "asyncio_default_fixture_loop_scope is unset" PytestDeprecationWarning.
# "function" matches the historical default that this suite was written
# against.
asyncio_default_fixture_loop_scope = function
